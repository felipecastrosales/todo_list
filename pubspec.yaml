name: todo_list
description: Todo List with Provider and SQLITE.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  flutter_signin_button: ^2.0.0
  google_fonts: ^2.1.0
  path: ^1.8.0
  provider: ^5.0.0
  sqflite: ^2.0.0+3

dev_dependencies:
  flutter_lints: ^1.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/
